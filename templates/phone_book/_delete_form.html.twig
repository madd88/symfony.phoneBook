<form method="post" action="{{ path('phone_book_delete', {'id': phone_book.id}) }}" onsubmit="return confirm('Вы действительно хотите удалить запись?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ phone_book.id) }}">
    <button class="btn btn-danger">Удалить</button>
</form>
